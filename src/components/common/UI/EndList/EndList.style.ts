import styled from 'styled-components'

const EndListWrapper = styled.div`
  width: 300px;

  padding: 10px;
  margin: 0 auto 20px;

  text-align: center;
  font-size: 16px;

  svg {
    margin-left: 10px;
  }
`

export default EndListWrapper
